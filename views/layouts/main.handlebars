<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<title>Stuffed Animal Rescue Aid Societe</title>
	<link rel="stylesheet" href="/css/base_styles.css">
	<!-- JavaScript Bundle with Popper -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>

</head>

<body>
	<header class="d-flex justify-content-between m-4">
		<div class="header-text d-flex justify-content-center" style="width: 100%;">
			<p>They were there for you...</p>
		</div>
		<a href="/"><img src="/assets/logo-1-en.svg" width="100" height="100" /></a>
		<div class="header-text d-flex justify-content-center" style="width: 100%;">
			<p>Now be there for them.</p>
		</div>
	</header>
	<div class="d-flex justify-content-center">
		{{#if logged_in}}
			{{#if hide_new_button}}
			{{else}}
			<a class="btn btn-block btn-primary" href="/listing/new/">rehome a friend</a>
			{{/if}}
		{{else}}
			<button class="btn btn-sm btn-outline-secondary" id="login" data-bs-toggle="modal" data-bs-target="#staticBackdrop">login to post</button>
		{{/if}}
	</div>
	<main class="container mt-4">
		{{{ body }}}
	</main>
	<footer class="d-flex justify-content-between">
		<div class="m-4">
			© 2021 <a class="text-secondary" style="text-decoration: none" href="https://n8blake.com">n8blake</a>
		</div>
		{{#if logged_in}}
		<button class="btn btn-sm btn-outline-secondary m-4" id="logout" >logout</button>
		{{/if}}	
	</footer>

<!-- Modal -->
<div class="modal fade" id="staticBackdrop" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1" aria-labelledby="staticBackdropLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="staticBackdropLabel">LOGIN</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <form>
        	<div class="mb-3">
			  <label for="email-login" class="form-label">Email address</label>
			  <input type="email" class="form-control" id="email-login" placeholder="name@example.com">
			</div>
			<div class="mb-3">
			  <label for="password" class="form-label">Password</label>
			  <input type="password" class="form-control" id="password-login" placeholder="password">
			</div>
        </form>
      </div>
      <div class="modal-footer d-flex justify-content-between">
      	<div class="form-check">
		  <input class="form-check-input" type="checkbox" value="" id="flexCheckDefault">
		  <label class="form-check-label" for="flexCheckDefault">
		    New Account
		  </label>
		</div>
		<div>
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <form class="login-form" style="display: inline;">
        	<button type="submit" class="btn btn-primary">Submit</button>
        </form>
    	</div>
      </div>
    </div>
  </div>
</div>

	{{#if logged_in}}
	<script src="/js/logout.js"></script>
	{{else}}
	<script src="/js/login.js"></script>
	{{/if}}
</body>

</html>
